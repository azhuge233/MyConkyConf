conky.config = {
    --Various settings
    background = true,                          -- forked to background
    cpu_avg_samples = 1,                        -- The number of samples to average for CPU monitoring.
    diskio_avg_samples = 1,                 -- The number of samples to average for disk I/O monitoring.
    double_buffer = true,                       -- Use the Xdbe extension? (eliminates flicker)
    if_up_strictness = 'address',               -- how strict if testing interface is up - up, link or address
    net_avg_samples = 1,                        -- The number of samples to average for net data
    no_buffers = true,                          -- Subtract (file system) buffers from used memory?
    temperature_unit = 'celsius',               -- fahrenheit or celsius
    text_buffer_size = 2048,                    -- size of buffer for display of content of large variables - default 256
    update_interval = 1,                        -- update interval
    imlib_cache_size = 0,                       -- disable image cache to get a new spotify cover per song


    --Placement

    alignment = 'bottom_right',                 -- top_left,top_middle,top_right,bottom_left,bottom_middle,bottom_right,
                                                -- middle_left,middle_middle,middle_right,none
    --Arch Duoscreen
    --gap_x = -1910,
    gap_x = 10,                                 -- pixels between right or left border
    gap_y = 5,                                 -- pixels between bottom or left border
    minimum_height = 200,                       -- minimum height of window
    minimum_width = 300,                        -- minimum width of window
    maximum_width = 500,                        -- maximum width of window

    --Graphical

    border_inner_margin = 5,                    -- margin between border and text
    border_outer_margin = 5,                    -- margin between border and edge of window
    border_width = 0,                           -- border width in pixels
    default_bar_width = 0,                      -- default is 0 - full width
    default_bar_height = 12,                    -- default is 6
    default_gauge_height = 12,                  -- default is 25
    default_gauge_width = 0,                    -- default is 40
    default_graph_height = 40,                  -- default is 25
    default_graph_width = 0,                    -- default is 0 - full width
    default_shade_color = '#000000',            -- default shading colour
    default_outline_color = '#000000',          -- default outline colour
    draw_borders = false,                       -- draw borders around text
    draw_graph_borders = true,                  -- draw borders around graphs
    draw_shades = false,                        -- draw shades
    draw_outline = false,                       -- draw outline
    stippled_borders = 0,                       -- dashing the border

    --Textual

    extra_newline = false,                      -- extra newline at the end - for asesome`s wiboxes
    format_human_readable = true,               -- KiB, MiB rather then number of bytes
    font = 'Roboto Mono:size=13',               -- font for complete conky unless in code defined
    max_text_width = 0,                         -- 0 will make sure line does not get broken if width too smal
    max_user_text = 16384,                      -- max text in conky default 16384
    override_utf8_locale = true,                -- force UTF8 requires xft
    short_units = true,                         -- shorten units from KiB to k
    top_name_width = 18,                        -- width for $top name value default 15
    top_name_verbose = false,                   -- If true, top name shows the full command line of  each  process - Default value is false.
    uppercase = false,                          -- uppercase or not
    use_spacer = 'none',                        -- adds spaces around certain objects to align - default none
    use_xft = true,                             -- xft font - anti-aliased font
    xftalpha = 1,                               -- alpha of the xft font - between 0-1

    --Windows

    own_window = true,                          -- create your own window to draw
    own_window_argb_value = 128,                -- real transparency - composite manager required 0-255
    own_window_argb_visual = true,              -- use ARGB - composite manager required
    own_window_colour = '#000000',              -- set colour if own_window_transparent no
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',  -- if own_window true - just hints - own_window_type sets it
    own_window_transparent = false,             -- if own_window_argb_visual is true sets background opacity 0%
    own_window_title = 'system_conky',          -- set the name manually  - default conky "hostname"
    own_window_type = 'normal',                 -- if own_window true options are: normal/override/dock/desktop/panel

    --Colours
    default_color = '#E95420',
    color1 = '#77216F',
    color2 = '#FFFFFF',
    color3 = '#000000',
};

conky.text = [[
${color6}${font Roboto:size=50}${alignc}${time %T}${font}

${color6}${font Roboto:size=15}${alignc}${time %Y 年 %-m 月 %d 日} ${time %A}${font}

${color1}${font Roboto:style=Bold:size=13}系 统   ${hr 2}${font}
${color}主机名:${color2}${alignr}${nodename}
${color}操作系统:${color2}${alignr}${texeci 3600 lsb_release -d | awk '{$1=""; print $0}'}
${color}内核版本:${color2}${alignr}${kernel}
${color}运行时间:${color2}${alignr}${uptime}
${color}总进程数:${color2}${alignr}${processes}

${color1}${font Roboto:style=Bold:size=13}C P U & G P U  ${hr 2}${font}
${color}CPU 使用量:${color2}${alignr}${cpu cpu0}%
${color}CPU 频率:${color2}${alignr}${freq_g} GHz
${color}CPU 温度:${color2}${alignr}${execi 1 sensors coretemp-isa-0000 | grep Package | cut -d "+" -f2 | cut -d " " -f1}
${color}CPU 0: ${color2}${cpubar cpu1 13,100} ${color}${alignr}CPU 1: ${color2}${cpubar cpu2 13,100}
${color}CPU 2: ${color2}${cpubar cpu3 13,100} ${color}${alignr}CPU 3: ${color2}${cpubar cpu4 13,100}
${color}CPU 4: ${color2}${cpubar cpu4 13,100} ${color}${alignr}CPU 5: ${color2}${cpubar cpu5 13,100}
${color}CPU 6: ${color2}${cpubar cpu6 13,100} ${color}${alignr}CPU 7: ${color2}${cpubar cpu7 13,100}
${color}GPU 频率:${color2}${alignr}${execi 1 cat /sys/class/drm/card1/gt_cur_freq_mhz} MHz
${color}CPU 使用量 Top 3 ${alignr}${offset -20}PID ${alignr}${offset -10}CPU% ${alignr}Mem%
${color2}${top name 1} ${alignr}${offset -25}${top pid 1}${alignr}${offset -15}${top cpu 1}${alignr}${top mem 1}
${color2}${top name 2} ${alignr}${offset -25}${top pid 2}${alignr}${offset -15}${top cpu 2}${alignr}${top mem 2}
${color2}${top name 3} ${alignr}${offset -25}${top pid 3}${alignr}${offset -15}${top cpu 3}${alignr}${top mem 3}

${color1}${font Roboto:style=Bold:size=13}内 存   ${hr 2}${font}
${color}内存使用量:${color2}${alignr}${mem}/${memmax}
${color}交换空间使用量:${color2}${alignr}${swap}/${swapmax}
${memgraph}
${color}内存使用量 Top 3 ${alignr}${offset -20}PID ${alignr}${offset -10}CPU% ${alignr}Mem%
${color2}${top_mem name 1} ${alignr}${offset -25}${top_mem pid 1}${alignr}${offset -15}${top_mem cpu 1}${alignr}${top_mem mem 1}
${color2}${top_mem name 2} ${alignr}${offset -25}${top_mem pid 2}${alignr}${offset -15}${top_mem cpu 2}${alignr}${top_mem mem 2}
${color2}${top_mem name 3} ${alignr}${offset -25}${top_mem pid 3}${alignr}${offset -15}${top_mem cpu 3}${alignr}${top_mem mem 3}

${color1}${font Roboto:style=Bold:size=13}存 储   ${hr 2}${font}
${color}存储空间:${color2}${alignr}${fs_used /}/${fs_size /}
${color}读 | 写:${color2}${alignr}${diskio_read /dev/sda}/s | ${diskio_write /dev/sda}/s
${color}存储空间使用量 Top 3 ${alignr}${offset -45}PID ${alignr}IO%
${color2}${top_io name 1} ${alignr}${offset -32}${top_io pid 1}${alignr}${top_io io_perc 1}
${color2}${top_io name 2} ${alignr}${offset -32}${top_io pid 2}${alignr}${top_io io_perc 2}
${color2}${top_io name 3} ${alignr}${offset -32}${top_io pid 3}${alignr}${top_io io_perc 3}

${color1}${font Roboto:style=Bold:size=13}网 络   ${hr 2}${font}
${color}网卡速率:${color2}${alignr}${execi 30 cat /sys/class/net/eno1/speed} Mb/s
${color}内网 IP:${color2}${alignr}${addrs eno1}
${color}网关:${color2}${alignr}${gw_ip}
${color}上传 | 下载 总量:${color2}${alignr}${totalup eno1} | ${totaldown eno1}
${color}上传 | 下载 速度:${color2}${alignr}${upspeed eno1}/s | ${downspeed eno1}/s
]];
